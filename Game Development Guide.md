# Series 制作ガイド（ジャンル横断版）

もぐらたたきに限らず、3つ消しパズル・15パズル・配管パズル・リバーシ・マインスイーパー・神経衰弱・脳トレ・間違い探し・進化ゲーム・ショップ系など、**ジャンルが違う前提**で共通化しています。

## 考え方

ゲームルールはジャンルごとに全く違うため、共通化できるのは「UIの外枠・進行の仕組み・演出」だけです。盤面の中身・ルール・判定は毎回そのゲーム専用に書きます。

## フォルダ構成

```
opgk-lab-games/
├── common/
│   ├── series-shell.js        ← 外枠・進行・演出（触らない）
│   └── series-style-base.css  ← 外枠の見た目（触らない）
├── games/
│   ├── 001_slide-puzzle/
│   │   ├── index.html         ← 共通読み込み＋固有ファイル読み込みのみ
│   │   ├── slide-style.css    ← このゲームのテーマカラー＋盤面レイアウト
│   │   └── slide-game.js      ← このゲームのルール・当たり判定
│   └── 002_juguchi-puzzle/    ← 配管パズル（実装例として参照用に常設）
│       ├── index.html
│       ├── juguchi-style.css
│       └── juguchi-game.js
├── index.html                 ← ゲーム一覧（トップページ）
├── about.html
└── README.md
```

## 共通（GameShell）が担当する範囲

| 機能 | 内容 |
|---|---|
| 外枠表示 | ブランド、タイトル、スコア/タイマー表示、フッター著作権、「← ゲーム一覧に戻る」リンク |
| ボタン | スタート・リセット・サウンドON/OFF |
| 隠しトリガー | タイトル5回タップ→激むず切替（プレイ中は不可） |
| 効果音 | `shell.playTone(周波数, 長さ, 波形)` |
| 演出 | `shell.showPopup(要素, テキスト, 'good'|'bad'|'bonus')` / `shell.toast(メッセージ)` |
| スコア | `shell.setScore()` / `shell.addScore()` / `shell.getScore()` |
| 進行管理 | タイマー使用時は自動カウントダウン。タイマーなしゲームは `shell.end('メッセージ')` を自分で呼んで終了させる |

## 各ゲームが必ず書くもの

1. `shell.board`（空のdiv）に自分で盤面を描画するJS
2. 盤面専用のCSS（グリッド、マスの形、色など）
3. `shell.onStart(...)`：スタート時に盤面を初期化してゲーム開始
4. `shell.onReset(...)`：リセット時に盤面を初期状態に戻す
5. （タイマー制の場合）`shell.onTimeUp(...)`：時間切れ時の結果表示
6. （激むず対応する場合）`shell.onHardModeChange(...)` または `shell.hardMode` を参照して難易度を変える

## 新しいジャンルを追加する時の設定例

`new GameShell({...})` の設定は、ジャンルによって以下のように変えます。

| ジャンル例 | hasTimer | hasScore | 終了のさせ方 |
|---|---|---|---|
| もぐらたたき系、脳トレ（赤いものタップ等） | true | true | 時間切れ（`onTimeUp`） |
| 3つ消しパズル、間違い探し | true または false | true | 時間切れ、またはクリアで `shell.end()` |
| 15パズル、配管パズル | false | false（任意） | 完成したら `shell.end('クリア！')` |
| リバーシ | false | true（両者の石数など） | 決着がついたら `shell.end('あなたの勝ち！')` |
| マインスイーパー | false（経過時間表示のみなら別途表示を自作） | false | 地雷を踏んだら `shell.end('ゲームオーバー')`、全マス開いたら `shell.end('クリア！')` |
| 神経衰弱 | false | true（一致数） | 全ペア揃ったら `shell.end('クリア！')` |
| 進化ゲーム、ショップ系 | 要件次第 | true | 要件次第（`shell.end()`をルールに応じて呼ぶ） |

`hasScore` / `hasTimer` を `false` にすると、その表示欄自体が出なくなります（例：15パズルはスコア表示が不要なら `hasScore:false`）。

## 実装Tips（じゃぐちパズル制作で得た知見）

パズル系（配管・迷路・15パズルなど、盤面を組み替えて完成させるタイプ）を作る際は、`games/002_juguchi-puzzle/juguchi-game.js` を実装例として参照してください。

- **盤面は正方形に**：横長/縦長になりがちなグリッドは `rows === cols` にして見た目を安定させる（1マスのサイズは縮小して調整してよい）。
- **必ず解けることを保証する**：ランダム生成のたびに「正解となる経路」をバックトラック法で先に敷き、その経路上のマスだけ正しい種類のパーツに差し替える。生成直後に「各マスが必要な向きを実現できる回転を持つか」を検証し、万一失敗したら再生成する（保険）。
- **パイプ等の描画はSVGが安全**：CSSの疑似要素だけで丸い曲がり角を作ると太さを上げたときに枠からはみ出しやすい。`<svg viewBox="0 0 100 100">` 内に `stroke-linecap: butt`（端はタイル境界にぴったり）＋`stroke-linejoin: round`（曲がり角だけ丸く）で描画すると、太さを変えても隣接タイルとの継ぎ目がズレない。
- **クリア演出「流れる」パターン**：接続判定のBFSで「入口から到達した順番」を記録しておき（`computeFlow()` → `order`配列）、その順に少しずつタイルへ `pipe-wet` クラスを付与していくと、水が流れていくような演出になる。スコア表示がないパズル系でも達成感を出せる汎用パターンとして流用可。
- **クリアサウンドは上昇アルペジオ推奨**：単発の2音（例：880Hz→440Hz）より、`523.25→659.25→783.99→1046.5`（ドミソド）のように短い間隔で連続再生する方が「クリアできた」喜びが伝わりやすい。

## 表記・文体ルール（全ゲーム共通・必ず遵守）

- **フォルダ名の命名**：`games/`直下のフォルダ名には番号を付ける（必須ではないが、詩さんが手動で管理しやすいよう原則付与する）。番号はカテゴリごとに帯を分ける。
  - 🧠 考える → 001番台（例：`001_slide-puzzle`、`002_juguchi-puzzle`）
  - ⚡ 反応する → 101番台（例：`101_kuma-tataki`）
  - 🧸 のんびり遊ぶ → 201番台（例：`201_hyakunin-isshu`）
- **ゲームタイトルの文字数**：一覧カードの`label`・ゲーム内タイトルとも、**絵文字アイコンを除いて8文字以内**とする（例：「じゃぐちパズル」は7文字でOK、「どうぶつ神経衰弱」は8文字でOK）。長い名前は短縮を検討する。
- **`<title>`タグ形式（絶対遵守）**：`ゲーム名+アイコン絵文字 | Casual Series`。「|」の前後には必ず半角スペースを入れる。
  - 正しい例：`<title>じゃぐちパズル🚰 | Casual Series</title>`
  - 絵文字を抜かさない・「|」前後のスペースを抜かさないこと
- **語尾の統一**：ゲーム内の指示・説明文は「〜しよう」ではなく「**〜しましょう**」で統一する（例：「水の道をつなげましょう」）
- **リセットボタン付近のヒント文（`shell.hint`）に激むずモードの話を絶対に書かない**：隠しモード切替（タイトル5回タップ→激むず）については、そのゲームのヒント文に一切登場させない。あくまでそのゲームの遊び方だけを書く。
  - ✕ NG例：「タイトルを5回タップすると激むずモードになります」のような文言を`hint`に入れる
  - ○ そのゲームの操作方法・目的だけを簡潔に書く
- **テーマカラーの参照元**：新ゲームのアクセントカラーを決める際は、必ずトップページ`index.html`内の各`game-card`の`--card-accent`値を確認し、既存ゲームと重複しない色を選ぶこと（`index.html`は常に最新版に差し替え運用されているため、このファイルが正）

## index.html（一覧ページ）の運用ルール

一覧ページはゲーム本体とは別に、独自の表示ルールを持っています。新作を追加するたびに、ここも意識する必要があります。

### カテゴリ内の並び順・表示数

- **降順固定**：各カテゴリ内は「番号が大きい（＝新しい）ものが先頭」。新作を追加したら、そのカテゴリの一番上に差し込む
- **最初の6枚だけ表示**、7枚目以降は`<details class="game-more">`（もっと見る）で畳む。JS不使用、ブラウザ標準機能のみ
- カテゴリが6本以下なら「もっと見る」自体を付けない

### バッジ（2種類のみ）

| バッジ | クラス名 | 色 | 用途 |
|---|---|---|---|
| 🆕 New | `card-badge-new` | オレンジ→ピンク | 各カテゴリの新着1本（目安） |
| 💡 推し | `card-badge-pickup` | 青系グラデーション | 詩さんが個人的に薦めたい1本（カテゴリごとに1枚目安、複数可） |

- 位置は左上・斜め8度のリボン形式（`.card-badge`共通）
- バッジは**位置とは無関係**（並び順を動かす目的では使わない）。降順で並べた結果、New/推しが自然と「もっと見る」内に埋もれることもあるが、それは仕様として許容する
- 目立たせたい場合は、対象カードを手動でカテゴリの先頭寄りに配置してよい（降順ルールを絶対視しすぎない）

### ピックアップ台（おすすめ台）

- ページ上部、カテゴリ一覧より前に独立した`.pickup-block`を設置
- 見出し：`🌿 ピックアップ（不定期更新）`（更新頻度を約束しない書き方で統一）
- 3〜4枚、カテゴリ横断で手動選出。カテゴリ本体からは削除せず、**あえて重複掲載**する（探しに来た人が迷子にならないようにするため）
- カード右上に`.cat-chip`（🧠/⚡/🧸の丸い絵文字チップ）を付け、どのカテゴリの子か分かるようにする
- カード内の`card-badge`（New/推し）はここでは表示しない（`.cat-chip`と重複するため、おすすめ台側では非表示にする）
- 見た目は「特別展示の額縁」イメージ：`.pickup-block`（外側・薄い縁取り）＋`.pickup-inner`（内側・クリーム背景）の二重構造。装飾は控えめに保つ（太さ・模様は都度好みで調整可）

### No.表示

- 各カードの`desc`の下・`play-btn`の上に、`<span class="game-no">No.XXX</span>`でフォルダ番号を表示
- 11px・グレー（`#9aa69e`）、目立たせない
- 表示する番号は**フォルダ名の番号**（`data-no`属性ではない。過去に一部ゲームで両者がズレているため注意）

### 説明文（desc）の文字数

- PC幅（カード固定286px）で1行に収まるよう、**全角20文字を目安の上限**にする
- モバイル幅（2列表示、カード幅約180px）は元々1〜2行に収まりやすいため、無理に1行に収めなくてよい
- 新作追加時、20文字を超えそうな場合は「〜しましょう」の直前で言い回しを削る（読点を削るだけで収まることも多い）

### グリッド仕様

- モバイル（〜699px）：2列固定
- タブレット（700px〜999px）：3列（画面幅に応じて可変）
- デスクトップ（1000px〜）：3列・カード幅286px固定＋中央寄せ（画面が広い分は左右の余白になる。4列以上には広げない）

## 注意：既存ゲームの改修時に起きやすいミス

既存ゲームのアプデを別スレッドで行う際、そのスレッド内に貼られた「過去の古いファイル」をそのまま参照してしまい、上記の最新ルール（文字数制限・title形式・hint内容など）が反映されないまま作業してしまうことがある。**新規ゲームを作る際は、必ず本ガイドの最新ルールを優先し、過去スレッドの実装例をそのまま踏襲しない。**

## 新作を作る手順

1. `games/002_juguchi-puzzle/`（または近いジャンルの既存ゲーム）フォルダをまるごとコピーして `games/新作名/` にリネーム
2. `index.html` の `<title>`（`ゲーム名+アイコン | Casual Series`形式、上記ルール参照）、CSS/JSファイル名の参照を新ファイル名に変更
3. `新作-style.css` にテーマカラー（トップページ`index.html`のカード一覧と重複しない色）と盤面レイアウトを書く
4. `新作-game.js` に `new GameShell({...})` の設定＋盤面生成＋ルールを書く（`common/`は一切編集しない）
5. 文言は「表記・文体ルール」に沿って統一する（語尾・タイトル文字数・hint内容を必ずチェック）
6. トップページ`index.html`に新作カードを追加する
   - 該当カテゴリの**先頭**に挿入（降順ルール）
   - `desc`は20文字以内目安
   - `<span class="game-no">No.XXX</span>`を追加
   - New/推しバッジを付けるか、ピックアップ台に載せるかは任意判断

## PJ資料の運用方針

- 資料に格納するのは基礎ファイルのみ：本ガイド、`common/series-shell.js`、`common/series-style-base.css`、`index.html`など
- 個別ゲームの実装例としては `games/002_juguchi-puzzle/` のみを参考資料として常設する
- それ以外の既存ゲームのコードは、修正が必要になった都度チャットへアップロードする方式とし、PJ資料には格納しない（ファイル容量・トークン節約のため）

## 今後の拡張候補（必要になったら検討）

- 難易度／初心者向けタグ（★表示など）：判断軸（難易度 or 初心者向き）がまだ言語化できていないため保留中
- スコアランキング → Google Sheets + GAS、または別途DB
- 公開 → GitHub Pages（運用中）/ Firebase Hosting
